{
  "name": "Opinionated zsh config",
  "id": "zsh",
  "version": "1.0.1",
  "description": "Opinionated zsh config",
  "options": {
    "omz_version": {
      "type": "string",
      "default": "master",
      "description": "The omz git refspec to checkout"
    },
    "omz_path": {
      "type": "string",
      "default": "/usr/local/oh-my-zsh",
      "description": "The path to clone omz to"
    },
    "pure_version": {
      "type": "string",
      "default": "main",
      "description": "The pure git refspec to checkout"
    },
    "pure_path": {
      "type": "string",
      "default": "/usr/local/pure-prompt",
      "description": "The path to clone omz to"
    }
  }
}
